<div class="container">
  <h1>Product show Page</h1>
  <h2><%= @product.name %></h2>
  <p>Made by: <%= @product.manufacturer %></p>
  <p>Description: <%= @product.description %></p>
  <p>Ingredients:<%= @product.ingredients %></p>
  <p>Size: <%= @product.bottle_size %></p>
  <p>Price: £<%= @product.price %></p>

  <%= link_to "All Sauces", products_path %>
  <%= link_to "Edit Sauce", edit_product_path(@product) %>
  <%= link_to "Delete Sauce", product_path(@product), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
  <%# <%= link_to "Add to Basket", product_order_products_path(@product) %> %>
  <%# <%= link_to("Add basket",  product_order_products_path(@product), data: {turbo_method: :post}) %> %>

  <div class="form-group mt-3">
    <%= simple_form_for [@product, @order_product] do |f| %>
      <%= f.button :submit, class:"btn btn-primary" %>
    <% end %>
  </div>
</div>

  <%# <%= button_to "Add basket",  product_order_products_path(@product), method: :post %>
  <%# data: {turbo_method: :delete} %>
<%# </div> %>
