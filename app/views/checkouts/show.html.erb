<!-- app/views/checkouts/show.html.erb -->
<div class="container">
  <div class="row py-4">
    <%# <div class="col-md-3"><%= image_tag @checkout.teddy.photo_url, width: '100%' %>
    <div class="col">
      <h1>Checkout #<%= @checkout.id %> <small><span class="badge badge-primary"><%= @checkout.state %></span></small></h1>
      <ul class="list-unstyled">
        <li><strong>ISSUED ON</strong> <%= @checkout.created_at.to_date %></li>
        <li><strong>TEDDY</strong> <%= @checkout.order.id %> [<%= @checkout.order_id %>]</li>
        <li><strong>TOTAL</strong> <%= humanized_money_with_symbol @checkout.amount_cents %></li>
        </li>
      </ul>
      <p>Estimated delivery in <strong>2 business days</strong></p>
    </div>
  </div>
</div>
